---
inspecode:

  global:

  tasks:

  tools:

    scalastyle:
    - config-file: ""   # use no config-file
      thresholds: {num-issues: 999}
    - # config-file: scalastyle-config.xml  # (auto-detect)
      thresholds: {num-issues: 999}
